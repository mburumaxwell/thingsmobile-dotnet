using System.Threading.Tasks;
using Xunit;

namespace ThingsMobile.Tests
{
    public class SimTests
    {
        //private readonly IThingsMobileClient simManager;

        public SimTests()
        {
            //var services = new ServiceCollection()
            //    .AddThingsMobileClient(userName: "", token: "");

            //var provider = services.BuildServiceProvider();

            //simManager = provider.GetRequiredService<IThingsMobileClient>();
        }

        [Fact]
        public void Works()
        {
            //var response = await simManager.GetCreditDetailsAsync();
            //var success = response.IsSuccessful;
            //var response = await simManager.UpdateSimTagAsync("", "");
            //var success = response.IsSuccessful;
        }
    }
}
